<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="boardDAO">
	<select id="list" resultType="boardVO">
		SELECT * FROM SNSBOARD
		ORDER BY regDate desc
	</select>
	<select id="search" parameterType="map" resultType="boardVO">
	 	SELECT * FROM SNSBOARD
			WHERE NAME LIKE '%'||#{Search}||'%' 
	
	</select>
	<select id="select" parameterType="int" resultType="boardVO">
		SELECT * FROM
		SNSBOARD WHERE num = #{num}
	</select>
	<select id="selectProfile" parameterType="String" resultType="memberVO">
		SELECT * FROM
		SNSMEMBER WHERE NAME = #{NAME}
	</select>
	<insert id="insert" parameterType="boardVO">
			INSERT INTO SNSBOARD
			(num, title, writer, password, content, regDate, images)
			VALUES
			(SNSBOARD_num.nextval,
					#{title}, #{writer}, #{password}, #{content}, SYSDATE, #{images})
			<selectKey keyProperty="num" resultType="Integer">
					SELECT NVL(MAX(num), 0) FROM SNSBOARD
			</selectKey>
	</insert>
	<select id="memberSearch"  parameterType="String" resultType="memberVO">
			SELECT * FROM SNSMEMBER
			WHERE NAME LIKE '%'||#{Search}||'%'
	</select> 
	<update id="update" parameterType="boardVO">
			UPDATE SNSBOARD  SET 
			title = #{title},
			content = #{content},
			images = #{images},
			WHERE email =#{email} and password = #{password}
	</update>
		<update id="updateProfile" parameterType="memberVO">
			UPDATE SNSBOARD  SET 
			password = #{password},
			email = #{email},
			profile = #{profile}
			WHERE email = #{email} <!-- AND pass = #{pass} -->
	</update>
	<update id="updateCount" parameterType="int">
			UPDATE SNSBOARD SET cnt = cnt + 1 WHERE num = #{num}<!-- 나중에 seq로 수정 -->
	</update>
	<delete id="delete" parameterType="boardVO">
			DELETE FROM SNSBOARD WHERE num = #{num}
			AND writer = #{writer}
	</delete>
	
	<delete id="deleteAll">
			DELETE FROM SNSBOARD
	</delete>
</mapper>